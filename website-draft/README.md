Draft portfolio scaffold

How to preview

1. Open `website-draft/index.html` in a browser (double-click or use a local server).

Optional: serve with Python from the folder:

```bash
cd "d:/Source Code/Portfolio/website-draft"
python -m http.server 8000
# then open http://localhost:8000
```

Next steps

- Replace placeholder name, bio, projects, and contact details in `index.html`.
- Add case study pages or link out to external project pages.
- Swap the avatar in `assets/avatar.svg` with your photo or illustration.
- I can make this a multi-page site or convert to a Jekyll/React template if you want.
